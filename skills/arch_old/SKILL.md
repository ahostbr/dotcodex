---
name: arch_old
description: "DEPRECATED — Use /arch instead. Legacy pre-consolidation architecture docs (backup). Only use for historical reference of the original per-app architecture."
---

# Legacy Architecture Reference (DEPRECATED)

> **Use `/arch` instead.** This skill points to the pre-consolidation docs from before the mega-app rewrite.

## Backup Location

`C:/Projects/docs/architecture-backup-2026-04-09/`

These docs describe the original per-app architecture (LiteCore hub + 9 satellite apps) and are preserved as historical reference only. The canonical docs are now at `C:/Projects/docs/architecture/` and loaded via `/arch`.
